# Lin Xia Portfolio

## Local development

Requires Node.js 18 or newer.

```bash
npm run dev
```

Then open `http://localhost:3000` in a browser.

To use another port:

```powershell
$env:PORT=4000; npm run dev
```

Portfolio content is managed in `script.js` through the `portfolioData` object.